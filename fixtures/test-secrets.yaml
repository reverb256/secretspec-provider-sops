# provider-rust end-to-end SOPS bridge — plaintext test fixture.
#
# This file is plaintext. CI step .github/workflows/ci.yml →
# "provider-rust end-to-end SOPS bridge (test fixture decrypt)" generates
# an ephemeral age keypair in /tmp, copies this file to the bridge test
# directory, encrypts it inline via `sops --encrypt --age <pub>`, then
# asserts that `secretspec-provider-sops get <encrypted> <key>` returns
# the expected plaintext value for each key.
#
# Values below are demo strings — they are NOT real secrets. If you change
# a value here, also update the expected array in the CI step's run block.

nvidia_api_key: nvapi-bridge-known
openai_api_key: sk-bridge-known
huggingface_token: hf-bridge-known
github_token: ghp-bridge-known
