# secrets.storage.yaml — Phase 2 SOPS target. S3 + restic + rclone + age.
#
# BACKUP_ENCRYPTION_KEY deliberately excluded from `keyring://` per
# sops-provider-design.md "Wallet key blocklist" — it's a master age
# encryption key whose compromise would expose the entire fleet's
# backups. Phase 3 final home: `onepassword://Homelab/Storage`.

garage_access_key: "REPLACE_ME_GARAGE_ACCESS_KEY"
garage_secret_key: "REPLACE_ME_GARAGE_SECRET_KEY"
restic_repo_password: "REPLACE_ME_RESTIC_REPO_PASSWORD"
rclone_config_passphrase: "REPLACE_ME_RCLONE_CONFIG_PASSPHRASE"
backup_encryption_key: "REPLACE_ME_BACKUP_ENCRYPTION_KEY"
