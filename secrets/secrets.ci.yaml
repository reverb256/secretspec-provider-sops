# secrets.ci.yaml — Phase 2 SOPS target. CI runtime only.
#
# Per migration-matrix.md: Phase 3 final home for these stays `env://`
# because they're consumed only at CI build time. The SOPS file exists
# only so the homelab CI runner can rotate them in version control.
#
# Replace REPLACE_ME_*, then `sops --encrypt --age $RECIPIENT --in-place`.

github_token: "REPLACE_ME_GITHUB_TOKEN"
github_runner_pat: "REPLACE_ME_GITHUB_RUNNER_PAT"
nix_packages_cache_token: "REPLACE_ME_NIX_PACKAGES_CACHE_TOKEN"
