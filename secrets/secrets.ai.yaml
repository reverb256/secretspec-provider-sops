# secrets.ai.yaml — Phase 2 SOPS target. Rotate before production.
#
# Replace each REPLACE_ME_* below with the real secret value, then:
#   sops --encrypt --age <recipient> --in-place secrets.ai.yaml
# The `sops://secrets.ai.yaml#<key>` URI in secretspec.toml will then
# resolve transparently through `secretspec-provider-sops`.
#
# Per migration-matrix.md Phase 2 routing; Phase 3 final homes are
# onepassword://Homelab/AI.

# Per-key placeholder content — replace with real values before encrypting.
nvidia_api_key: "REPLACE_ME_NVIDIA"
openai_api_key: "REPLACE_ME_OPENAI"
anthropic_api_key: "REPLACE_ME_ANTHROPIC"
openai_org_id: "REPLACE_ME_OPENAI_ORG_ID"
huggingface_token: "REPLACE_ME_HUGGINGFACE"
stability_api_key: "REPLACE_ME_STABILITY"
replicate_api_token: "REPLACE_ME_REPLICATE"
